# Avances Día 28-03-2026

## Objetivos Alcanzados:
1. **Dashboard SCADA**:
    - Creación de interfaz premium con glassmorphism (`public/css/style.css`, `public/index.html`).
    - Consumo asíncrono vía `app.js` mediante la API de control HTTP.
    - Controles interactivos para armar/desarmar, inyectar fallos de sistema (AC, Battery, Tamper), consultar logs de eventos y operar en **Modo Laboratorio**.
2. **Soporte de Pasarela RS232**:
    - Instalación de la biblioteca `serialport`.
    - Implementación de un `SerialServer` que levanta la conexión serial a partir de las nuevas variables de entorno (`SIM_SERIAL_PORT` / `SIM_SERIAL_BAUDRATE`).
    - Lógica de `SerialBroadcaster` que expulsa los eventos de sistema idénticamente a como lo hace el socket TCP, reutilizando el `AsciiEncoder`.
3. **Consola Visual RS232 (Salida al CIE-H14)**:
    - Implementación Back/Front de una terminal negra fosforescente en vivo para monitorizar los datos de salida ("tipo Matrix").
    - Endpoint REST `GET /serial/console` con historial circular (100 líneas) en el `SerialServer`.
    - Polling en JS para renderizar RX (Recibido - amarillo) y TX (Transmitido - azul claro) con auto-scroll.
4. **Manual de Usuario**:
    - Redacción completa del archivo `MANUAL_DASHBOARD.md` con las explicaciones del Dashboard, RS232, Modo Laboratorio y Consola Visual.

## Próximos pasos recomendados:
- Probar RS232 conectando físicamente el Solae CIE-H14 o usando puertos serie virtuales (`com0com` en Windows).
- Integración Opcional: Escribir tests unitarios que simulen mensajes RS232 corrompidos o fallos eléctricos durante el stream de `serialport` para afianzar resiliencia.

## Resolución de Bugs y Ajustes Finales
- Se fijó la consola RS232 (`src/control-api/routes/serial.ts`) eliminando la limitación donde si el puerto estaba "Cerrado" (`serialServer.isRunning() == false`) la memoria de LOGs devolvía un array vacío `[]`. Ahora los tráficos generados de TX hacia Solae CIE-H14 en modo virtual (con simulaciones) se observan en la consola sin requerir puerto hardware instanciado.
